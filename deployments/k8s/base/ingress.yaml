apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frappe-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: frappe.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frappe-frontend
                port:
                  number: 8080
